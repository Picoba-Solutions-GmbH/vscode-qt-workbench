pragma ComponentBehavior: Bound

import QtQuick

// A plain surface panel. Every view groups its content into these.
Rectangle {
    color: AppTheme.surface
    radius: 12
    border.width: 1
    border.color: AppTheme.border
}
